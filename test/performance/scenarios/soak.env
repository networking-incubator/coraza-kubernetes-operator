# Soak: hold a moderate rate for a long duration to expose memory growth / GC in
# the Envoy WASM VM and the operator cache. Pair with periodic collect.sh pulls.
QPS="300"
DURATION="3600s"   # 1h; raise to 4h for a full soak
CONNS="16"
REQ_PATH="/"
PAYLOAD_SIZE="0"
