# Latency at a fixed, sub-saturation rate. Open-model constant QPS is the right
# way to measure added WAF latency (avoids coordinated omission). Pick a QPS
# comfortably below the B0 knee found with benign-max.
QPS="500"
DURATION="120s"
CONNS="16"
REQ_PATH="/"
PAYLOAD_SIZE="0"
